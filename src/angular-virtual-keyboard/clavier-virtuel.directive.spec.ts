import { ClavierVirtuelDirective } from './clavier-virtuel.directive';

describe('ClavierVirtuelDirective', () => {
  it('should create an instance', () => {
    const directive = new ClavierVirtuelDirective();
    expect(directive).toBeTruthy();
  });
});
